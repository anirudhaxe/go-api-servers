version: "2"
sql:
  - engine: "postgresql"
    schema: "repository/sql/schema.sql"
    queries: "repository/sql/queries"
    gen:
      go:
        package: "repository"
        out: "repository"
        sql_package: "pgx/v5"
